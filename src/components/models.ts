export interface Todo {
  id: number
  content: string
}

export interface Meta {
  totalCount: number
}
export interface ProfileMenuOption {
  text: string
  link: string
  className: string
  active: boolean
}
